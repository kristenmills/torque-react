import PropTypes from 'prop-types';
import React from 'react';

const TorqueSlide = ({ children }) => children;

TorqueSlide.propTypes = {
  duration: PropTypes.number,
};

export default TorqueSlide;
